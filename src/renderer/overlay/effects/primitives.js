// Shared primitive types for the overlay effects.
//
// Glitch rendering now lives in the per-theme modules under `./glitches/`, and
// cursor-trail update/draw lives in `./cursorTrail.ts`. This module retains just
// the small shared type below.
export {};
