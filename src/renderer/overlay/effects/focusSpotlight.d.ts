import type { OverlayState } from '../../shared/types';
export declare function drawFocusSpotlight(ctx: CanvasRenderingContext2D, W: number, H: number, locusX: number, locusY: number, strength: number, // 0..1, eased by the engine
state: OverlayState): void;
