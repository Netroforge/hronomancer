import type { OverlayState } from '../../shared/types';
export declare function drawScanlines(ctx: CanvasRenderingContext2D, W: number, H: number, state: OverlayState, time: number, intensityOverride?: number): void;
